{% set name = "crystal-toolkit" %}
{% set version = "2025.12.16" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/crystal_toolkit-{{ version }}.tar.gz
  sha256: 35018f116577e6bb1392dae6f75ad1f3e6b773ea4473e08c6f1cdae799ead8e7

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - setuptools >=45
    - setuptools-scm >=6.2,<8
    - jupyter_packaging
    - pip
  run:
    - python >={{ python_min }}
    - crystaltoolkit-extension
    - dash-mp-components >=0.4.38
    - dash >=2.11.0
    - flask-caching
    - frozendict
    - pymatgen
    - scikit-image
    - scikit-learn
    - shapely
    - webcolors
    - pydantic-settings
    - mp-api
    - jupyterlab >=3

test:
  imports:
    - crystal_toolkit
  commands:
    - pip check
  requires:
    - pip
    - python {{ python_min }}

about:
  home: https://github.com/materialsproject/crystaltoolkit
  summary: Crystal Toolkit is a web app framework from the Materials Project
  license: BSD-3-Clause
  license_file:
    - LICENSE
    - jupyterlab-extension/LICENSE

extra:
  recipe-maintainers:
    - awvwgk
